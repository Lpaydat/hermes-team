{
  "id": "debug-fix",
  "name": "Debug fix subworkflow — reproduce, fix, verify",
  "description": "Child workflow called by tech-lead-execute when tests fail. Debugger reproduces and fixes, verifier independently checks.",
  "nodes": [
    {
      "id": "debug",
      "profile": "debugger",
      "skill": "debug-loop",
      "body_template": "## Bug: tests failing after merge\n\n**Repo:** ${repo}\n**Failing tests:** ${failing_tests}\n**Context:** ${context}\n\nInvestigate the root cause. Run your debug-loop (reproduce → fix → converge). Write a regression test. Fix the root cause.\n\nThe repo is at: ${repo}\nWork on the current branch (do NOT create a new branch — the code is already on master).\n\nComplete with metadata:\n```\n{verdict: \"fixed\"|\"escalated-design\"|\"blocked-hitl\", fix_commit: \"<sha>\", root_cause_summary: \"<one-line>\", changed_files: [\"...\"]}\n```",
      "title_template": "[bug] Fix failing tests: ${bug_title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "fixed",
                "escalated-design",
                "blocked-hitl"
              ]
            },
            "fix_commit": {
              "type": "string"
            },
            "root_cause_summary": {
              "type": "string"
            },
            "changed_files": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    {
      "id": "verify-fix",
      "profile": "verifier",
      "skill": "adversarial-review",
      "body_template": "## Verify the debugger's fix\n\n**Repo:** ${repo}\n**Debugger verdict:** ${nodes.debug.output.verdict}\n**Fix commit:** ${nodes.debug.output.fix_commit}\n**Root cause:** ${nodes.debug.output.root_cause_summary}\n\nThe debugger claims the bug is fixed. Verify independently:\n\n1. Run ALL tests at ${repo}: `pytest -q`\n2. Check the regression test the debugger wrote actually fails without the fix (mutation check)\n3. Falsify: try to break the fix with edge cases\n\nComplete with metadata:\n```\n{verdict: \"PASS\"|\"FAIL\", evidence: \"command + exit code\", all_tests: N}\n```",
      "title_template": "[verify] Debug fix: ${bug_title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "PASS",
                "FAIL"
              ]
            },
            "evidence": {
              "type": "string"
            },
            "all_tests": {
              "type": "integer"
            }
          }
        }
      },
      "depends_on": [
        "debug"
      ]
    }
  ],
  "edges": [
    {
      "from": "debug",
      "to": "verify-fix"
    }
  ]
}