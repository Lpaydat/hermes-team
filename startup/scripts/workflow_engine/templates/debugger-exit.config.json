{
  "max_debug_iterations": 10,
  "description": "Change this value to control how many re-verify cycles the debugger-exit workflow allows before escalating to PO. Must match the max_iterations values in debugger-exit.json edges."
}
