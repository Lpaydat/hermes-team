{
  "id": "tech-lead-execute",
  "name": "Tech-Lead Execute — plan (loop_engine), verify (behavior), fix, close",
  "description": "Version B: tech-lead decomposes spec into tasks, then calls loop_engine with one phase per task (execution=developer, verifier=verifier). loop_engine handles all iteration internally. No kanban_chains in plan phase.",
  "trigger": {
    "source": "card_completed",
    "condition": {
      "assignee": "tech-lead",
      "status": "done",
      "title_prefix_any": [
        "[spec]",
        "[ticket-"
      ]
    }
  },
  "nodes": [
    {
      "id": "plan",
      "profile": "tech-lead",
      "skill": "loops-engineering",
      "body_template": "## Pure loop_engine — decompose + execute\n\n**Spec card:** ${trigger.card_id}\n**Title:** ${trigger.title}\n\nYou are the tech-lead. DO NOT grill the user.\n\n### Phase 1: Decompose\n\nRead the spec. Load your `to-tickets` skill. Decompose into atomic tasks. Each task needs: title, acceptance criteria (testable), dependencies on other tasks, sizing.\n\nGuidelines for atomic tasks:\n- A junior dev can complete it in one sitting\n- It has clear, testable acceptance criteria\n- It can be verified independently\n- It's not two tasks disguised as one\n- Split by independent concern, not by technical layer\n- For wide specs (many independent features), create separate tasks per feature\n\n### Phase 2: Execute via loop_engine (CRITICAL)\n\nCall `loop_engine` with:\n- `goal`: the spec title\n- `runner`: \"tech-lead\"\n- `phases`: one phase per task. Order them by dependency. Each phase has:\n  - `execution`: {title: \"[task] <title>\", body: \"Implement the acceptance criteria below. Write tests. Run ALL tests. When ALL tests pass, COMPLETE this card (do NOT block for review — the verifier handles review independently).\\n\\n<acceptance criteria + constraints>\", assignee: \"developer\"}\n  - `verifier`: {title: \"[verify] <title>\", body: \"Verify the developer's work against ALL acceptance criteria. Write behavior tests against the public interface and execute them. Do NOT edit dev code.\", assignee: \"verifier\", metric_type: \"ground_truth\"}\n  - `max_iterations`: 5\n\nloop_engine will:\n- Execute each phase's execution card (developer builds)\n- Run the verifier card (verifier checks)\n- advance → next phase, replan → re-execute, escalate → stop\n- Dependency-park YOUR card until ALL phases converge\n\nDO NOT call kanban_chains. DO NOT call kanban_create. loop_engine handles card creation.\n\nGIT SAFETY: Before writing task cards, run `git rev-parse main` to get the current HEAD. Use that commit for any baseline reference. Do NOT trust commit SHAs from the spec card body — they may be stale. Always re-resolve.\n\nCRITICAL GIT SAFETY: Do NOT run `git rebase` or `git reset --hard` on any branch. These destroy commits that cannot be recovered. Use only `git add`, `git commit`, and `git merge`. If you need to undo a change, use `git checkout -- <file>` (discards uncommitted changes only, never touches history).\n\nAfter loop_engine converges (you auto-promote): read the verifier summaries. Complete with plan metadata.\n\n**Completion metadata:**\n```\n{plan_complete: true, task_count: N, phases_converged: N, sizing_summary: \"small:N medium:N large:N\"}\n```",
      "title_template": "[tl-b] Plan: ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "plan_complete",
            "task_count"
          ],
          "properties": {
            "plan_complete": {
              "type": "boolean"
            },
            "task_count": {
              "type": "integer"
            },
            "phases_converged": {
              "type": "integer"
            },
            "sizing_summary": {
              "type": "string"
            }
          }
        }
      }
    },
    {
      "id": "verify",
      "profile": "verifier",
      "skill": "adversarial-review",
      "body_template": "## Adversarial Behavior Verification\n\n**Spec card:** ${trigger.card_id}\n**Tasks completed:** ${nodes.plan.output.task_count}\n\nYour goal: TRY TO BREAK the integrated code. Prove the developer's claims are true by EXECUTING behavior tests — not by reading code.\n\n### Phase 1: Spec → Behavior Test Matrix\n\nRead the spec. Extract EVERY requirement. For each, write a BEHAVIOR test that proves it works through the PUBLIC INTERFACE:\n- CLI tools: pipe input, check stdout/stderr/exit code\n- REST APIs: HTTP requests, check status codes + response bodies\n- Libraries: import and call public functions, check return values\n\nDo NOT test implementation details. Test BEHAVIOR — what the user sees. These tests must survive refactors.\n\nWrite all behavior tests to a file. Use the project's language and test framework:\n- Python: `tests/test_behavior.py` (pytest)\n- Rust: `tests/behavior.rs` (cargo test)\n- TypeScript: `src/behavior.test.ts` (vitest)\n- Go: `behavior_test.go` (go test)\n\n### Phase 2: Attack Your Own Tests (CRITICAL)\n\nYour tests have gaps. LLMs write impressive docstrings then use safe inputs that never actually exercise the edge case. Attack your own work.\n\nRead EVERY test you just wrote. For each test, ask these PRINCIPLES (not a fixed checklist — reason about what applies to THIS specific code):\n\n1. **Honesty check**: Does the test input actually match what the test claims to test? If the docstring says 'special characters' but the input is safe — that's a lie. Fix it. Put the actual edge-case value in the input.\n2. **Adversarial thinking**: For every input the code accepts, what is the WORST input a hostile user could provide? Not just invalid — adversarial. Write tests for those inputs.\n3. **Independence**: Does the test verify behavior the spec requires, or behavior the test author assumed? Remove assumptions. Test only what the spec guarantees.\n4. **Completeness**: Walk through every code path — happy path, error path, edge path. Is every path tested? If not, write the missing test.\n5. **Deployment readiness**: Does the code run the same way in production as in tests? Test fixtures and test configuration can mask deployment-only failures.\n\nWrite ADDITIONAL tests for every gap you find. Append them to the same test file.\n\n### Phase 3: Execute — Run ALL Tests\n\nRun ALL tests: developer's existing tests AND your behavior tests AND your attack tests. Any failure is a finding.\n\n### Phase 3.5: Lint + Format Check (Code Quality Gate)\n\nRun the project's linter and formatter. These were set up by the scaffold node before tickets started.\n\nIf a Makefile exists, run `make lint` and `make format-check`. Otherwise detect the language:\n- Python: `ruff check .` and `ruff format --check .`\n- Rust: `cargo clippy -- -D warnings` and `cargo fmt --check`\n- TypeScript: `npx biome check .`\n- Go: `golangci-lint run` and `gofmt -l .`\n\nRules:\n- Lint warnings are FINDINGS (not failures). Report them but they don't block PASS.\n- Lint ERRORS (ruff F, clippy -D warnings) are FAILURES. The code has real quality issues.\n- Format mismatches are FINDINGS. Report them.\n- If the project has NO linter configured (no Makefile, no ruff.toml/clippy.toml/biome.json), SKIP this phase and note it in the output.\n\n### Phase 4: Do NOT Edit Dev Code\n\nYou must NOT modify the developer's code. You only write tests AGAINST it.\n\n**Verdict:**\n- PASS: ALL behavior tests passed. Every spec requirement proven by execution.\n- FAIL: One or more behavior tests failed. Findings are the failing tests + their output.\n- ESCALATE: spec is ambiguous or impossible to test.\n\n**Counting rule**: behavior_tests_passed and behavior_tests_total MUST be integers. Count the actual tests in your test file using `pytest --collect-only -q | wc -l` or equivalent. If some tests fail, behavior_tests_passed is the number that passed (NOT the total, NOT a string description).\n\n**Completion metadata:**\n```\n{verdict: \"PASS\"|\"FAIL\"|\"ESCALATE\", findings_count: N, behavior_tests_total: N, behavior_tests_passed: N, behavior_test_file: \"path\", production_mode_tested: true}\n```",
      "title_template": "[verify-b] Integration: ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict",
            "findings_count",
            "behavior_tests_total",
            "behavior_tests_passed",
            "production_mode_tested"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "PASS",
                "FAIL",
                "ESCALATE"
              ]
            },
            "findings_count": {
              "type": "integer"
            },
            "behavior_tests_total": {
              "type": "integer"
            },
            "behavior_tests_passed": {
              "type": "integer"
            },
            "behavior_test_file": {
              "type": "string"
            },
            "production_mode_tested": {
              "type": "boolean"
            }
          }
        }
      },
      "depends_on": [
        "plan"
      ]
    },
    {
      "id": "fix",
      "profile": "developer",
      "skill": "",
      "body_template": "## Fix findings from behavior verification\n\n**Behavior test file:** ${nodes.verify.output.behavior_test_file}\n\nThe verifier wrote behavior tests designed to break your code. Fix YOUR code so ALL tests pass. Do NOT modify the verifier's tests.\n\nRun ALL tests. Every test must pass.\n\n**Completion metadata:**\n```\n{fixed: true, findings_fixed: N, all_tests_pass: N, total_tests: N}\n```",
      "title_template": "[fix-b] Behavior findings: ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "fixed",
            "findings_fixed",
            "all_tests_pass",
            "total_tests"
          ],
          "properties": {
            "fixed": {
              "type": "boolean"
            },
            "findings_fixed": {
              "type": "integer"
            },
            "all_tests_pass": {
              "type": "integer"
            },
            "total_tests": {
              "type": "integer"
            }
          }
        }
      }
    },
    {
      "id": "re-verify",
      "profile": "verifier",
      "skill": "adversarial-review",
      "body_template": "## Re-verify — adversarial behavior testing after fix\n\n**Spec card:** ${trigger.card_id}\n**Fix card:** ${nodes.fix.output.findings_fixed} findings fixed\n**Previous run:** ${nodes.verify.output.behavior_tests_passed}/${nodes.verify.output.behavior_tests_total} behavior tests passed\n\nThe developer claims all findings are fixed. Your job: PROVE IT.\n\n### Phase 1: Re-run behavior tests\n\nRun your behavior tests from the first verify. Every previously-failing test must now PASS.\n\n### Phase 2: Attack Your Own Tests Again\n\nThe fix may have introduced NEW bugs. Apply the same adversarial self-review principles:\n1. **Honesty check**: Does each test input actually match what it claims?\n2. **Adversarial thinking**: What new worst-case input would the fix be vulnerable to?\n3. **Independence**: Are you testing behavior, or assumptions about the fix?\n4. **Completeness**: Did the fix close one door but open another?\n5. **Deployment readiness**: Test the fix under production configuration — test fixtures can mask deployment-only regressions.\n\n### Phase 3: Run ALL Tests\n\nRun ALL tests — developer's AND your behavior tests AND your attack tests. Every previously-failing test must now PASS.\n\n### Phase 3.5: Re-run Lint + Format Check\n\nRe-run the project's linter and formatter to verify the fix didn't introduce new lint errors.\n\nIf a Makefile exists, run `make lint` and `make format-check`. Otherwise detect the language:\n- Python: `ruff check .` and `ruff format --check .`\n- Rust: `cargo clippy -- -D warnings` and `cargo fmt --check`\n- TypeScript: `npx biome check .`\n- Go: `golangci-lint run` and `gofmt -l .`\n\nIf the project has NO linter configured, SKIP this phase.\n\n### Phase 4: Do NOT Edit Dev Code\n\n**Verdict:**\n- PASS: ALL behavior tests pass (old + new).\n- FAIL: Some behavior tests still failing.\n- ESCALATE: cannot resolve.\n\n**Completion metadata:**\n```\n{verdict: \"PASS\"|\"FAIL\"|\"ESCALATE\", findings_count: N, behavior_tests_total: N, behavior_tests_passed: N, production_mode_tested: true}\n```",
      "title_template": "[re-verify-b] Integration: ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict",
            "findings_count",
            "behavior_tests_total",
            "behavior_tests_passed",
            "production_mode_tested"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "PASS",
                "FAIL",
                "ESCALATE"
              ]
            },
            "findings_count": {
              "type": "integer"
            },
            "behavior_tests_total": {
              "type": "integer"
            },
            "behavior_tests_passed": {
              "type": "integer"
            },
            "production_mode_tested": {
              "type": "boolean"
            }
          }
        }
      }
    },
    {
      "id": "close",
      "profile": "tech-lead",
      "skill": "loops-engineering",
      "body_template": "## Close — merge ticket branch + verify integration\n\n**Plan:** ${nodes.plan.output.task_count} tasks planned\nVerify: ${nodes.verify.output.verdict}\nRe-verify (if ran): ${nodes.re-verify.output.verdict}\n\n### Step 1: Merge the ticket branch into the project main branch\n\nThis ticket's code lives on a git branch. It must be merged into the project's main branch before this card completes.\n\n```\ngit checkout master\ngit merge --no-ff <ticket-branch-name>\n```\n\nIf there are merge conflicts:\n1. Resolve them manually (prefer the ticket branch's changes for new features, master's changes for existing infrastructure)\n2. Run ALL tests\n3. If tests fail after conflict resolution: ESCALATE (do NOT force-complete)\n\n### Step 2: Run ALL tests on the merged result\n\nUse the project's test runner. If a Makefile exists, run `make check`. Otherwise detect the language:\n- Python: `pytest -q`\n- Rust: `cargo test`\n- TypeScript: `npm test` or `npx vitest`\n- Go: `go test ./...`\n\n- If ANY test fails: ESCALATE with verdict=escalated, escalate_reason=\"tests failed after merge\"\n- If ALL tests pass: proceed to step 3\n\n### Step 3: Verify no work was lost\n\n```\ngit log master..<ticket-branch-name>\n```\n\nThis should output NOTHING (all commits merged). If it shows commits, the merge failed — ESCALATE.\n\n### Step 4: Complete\n\n- All tests pass on merged master: complete with verdict=merged\n- Tests failed or merge impossible: complete with verdict=escalated\n\n**Completion metadata:**\n```\n{verdict: \"merged\"|\"escalated\", tasks_planned: ${nodes.plan.output.task_count}, tasks_completed: ${nodes.plan.output.task_count}, escalate_reason: \"\", merged_to: \"master\", all_tests_pass: true|false}\n```",
      "title_template": "[tl-b] Close: ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "merged",
                "escalated"
              ]
            },
            "tasks_planned": {
              "type": "integer"
            },
            "tasks_completed": {
              "type": "integer"
            },
            "escalate_reason": {
              "type": "string"
            }
          }
        }
      }
    },
    {
      "id": "merge-verify",
      "profile": "verifier",
      "skill": "adversarial-review",
      "body_template": "## Merge verification — mechanical git + test check\n\n**Ticket:** ${trigger.title}\n**Close verdict:** ${nodes.close.output.verdict}\n\n### Your job: PROVE the merge is real, not just claimed.\n\nRun these checks. Each must PASS. Do NOT take the close node's word for it.\n\n### Check 1: Branch is fully merged\n\n```\ngit log master..<ticket-branch-name>\n```\n\nThis must output NOTHING (empty). If any commits appear, the branch was NOT merged — FAIL.\n\nIf you don't know the branch name, find it:\n```\ngit branch --list 'ticket-*'\n```\n\n### Check 2: No dangling/destroyed commits\n\n```\ngit fsck --unreachable 2>/dev/null | grep commit\n```\n\nIf this shows ANY commits containing 'ticket' or 'fix' in their message — FAIL. Work was destroyed.\n\n### Check 3: Tests pass on merged master\n\n```\ngit checkout master\n```\n\nIf a Makefile exists, run `make check`. Otherwise detect the language:\n- Python: `pytest -q`\n- Rust: `cargo test`\n- TypeScript: `npm test` or `npx vitest`\n- Go: `go test ./...`\n\nALL tests must pass. ANY failure — FAIL.\n\n### Check 4: No stray worktrees with unmerged work\n\n```\ngit worktree list\ngit stash list\n```\n\nIf there are worktrees or stashes with ticket-related content that isn't in master — FAIL.\n\n### Verdict\n\n- ALL checks pass: verdict=PASS\n- ANY check fails: verdict=FAIL with specific evidence\n\n**Completion metadata:**\n```\n{verdict: \"PASS\"|\"FAIL\", branch_merged: true|false, no_dangling: true|false, tests_pass: true|false, all_tests: N, findings: [\"specific issue 1\", ...]}\n```",
      "title_template": "[merge-verify] ${trigger.title}",
      "output": {
        "schema": {
          "type": "object",
          "required": [
            "verdict"
          ],
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "PASS",
                "FAIL"
              ]
            },
            "branch_merged": {
              "type": "boolean"
            },
            "no_dangling": {
              "type": "boolean"
            },
            "tests_pass": {
              "type": "boolean"
            },
            "all_tests": {
              "type": "integer"
            },
            "findings": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    }
  ],
  "edges": [
    {
      "from": "plan",
      "to": "verify",
      "condition": "${nodes.plan.output.plan_complete} exists"
    },
    {
      "from": "verify",
      "to": "close",
      "condition": "${nodes.verify.output.verdict} == 'PASS' OR ${nodes.verify.output.verdict} == 'ESCALATE'"
    },
    {
      "from": "verify",
      "to": "fix",
      "condition": "${nodes.verify.output.verdict} == 'FAIL'",
      "max_iterations": 10
    },
    {
      "from": "fix",
      "to": "re-verify",
      "max_iterations": 10
    },
    {
      "from": "re-verify",
      "to": "close",
      "condition": "${nodes.re-verify.output.verdict} == 'PASS' OR ${nodes.re-verify.output.verdict} == 'ESCALATE'"
    },
    {
      "from": "re-verify",
      "to": "fix",
      "condition": "${nodes.re-verify.output.verdict} == 'FAIL'",
      "max_iterations": 10
    },
    {
      "from": "close",
      "to": "merge-verify",
      "condition": "${nodes.plan.output.task_count} > 1"
    }
  ]
}